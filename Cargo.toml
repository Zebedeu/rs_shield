[workspace]
members = [
    "rsb-cli",
    "rsb-core",
    "rsb-desktop",
]
resolver = "2"

[patch.crates-io]
time = { version = "0.3.47" }

[profile.release]
debug = false
lto = "thin"
opt-level = 3
codegen-units = 1
strip = true
panic = "abort"
